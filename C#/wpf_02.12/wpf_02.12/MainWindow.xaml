<Window x:Class="wpf_02._12.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:wpf_02._12"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <Label Content="Search:"/>
            <TextBox x:Name="txtSearch" TextChanged="TxtSearch_TextChanged"/>
        </StackPanel>

        <ListBox x:Name="lbItems" Grid.Row="1" SelectionChanged="LbItems_SelectionChanged"/>

        <StackPanel Grid.Row="2" Margin="0,10">
            <Label Content="Input mező:"/>
            <TextBox x:Name="txtInput"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button x:Name="btnSave" Content="Save" Width="80" Margin="5" Click="BtnSave_Click"/>
            <Button x:Name="btnDelete" Content="Delete" Width="80" Margin="5" Click="BtnDelete_Click"/>
        </StackPanel>
    </Grid>
</Window>
